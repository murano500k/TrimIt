<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextAccent</item>
        <item name="android:editTextBackground">@drawable/et_bg</item>
        <item name="android:editTextColor">@color/colorTextAccent</item>
        <item name="android:textColorHint">@color/colorTextPrimary</item>
    </style>
</resources>
